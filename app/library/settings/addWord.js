define(
    ['options', 'vendor/jquery', 'vendor/underscore'],
    function(options, $, _){
        function render(){
            return '';
        }

        return {
            render: render
        };
    }
);